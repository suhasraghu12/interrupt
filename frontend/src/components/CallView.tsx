// Mic capture, LiveKit room connect, agent audio playback (F1). The main call UI.

export function CallView() {
  // TODO(week 1): connect/disconnect controls, mic mute, playback of agent audio.
  return <div>CallView placeholder</div>;
}
