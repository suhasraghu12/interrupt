// Live partial + final transcript display (F3), streamed from the backend.

export function TranscriptPanel() {
  // TODO(week 1): subscribe to transcript events over the data channel, render.
  return <div>TranscriptPanel placeholder</div>;
}
