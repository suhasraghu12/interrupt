// F10: live p50/p95 TTFT and end-to-end response time, driven by telemetry events
// streamed from the backend. Not implemented until week 5.

export function LatencyDashboard() {
  // TODO(week 5): subscribe to TurnStageEvent stream, compute rolling p50/p95, chart.
  return <div>LatencyDashboard placeholder</div>;
}
